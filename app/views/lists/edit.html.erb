<body>
  <h1>Editing Book</h1>

<%= form_with model: @book, url: '/books', method: :post do |f| %>
<p>title</p>
<%= f.text_field :title %>

<p>Body</p>
<%= f.text_area :body %>

<p><%= f.submit 'Update Book' %></p>
  
    <%= link_to "Show", list_path(@book.id) %>
    |
    <%= link_to "Back", lists_path %>
    
<% end %>
</body>
